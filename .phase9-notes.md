# Phase 9 Implementation Notes (internal)

## DONE since last update
- [x] socket.ts: project:join (validates owner via projects.userId OR collaborator status accepted/pending) and project:leave handlers; broadcastProjectMessage, broadcastProjectMissionUpdate added
- [x] routers.ts: collaboration endpoints added to projects router: inviteCollaborator (sends in-app notif to invitee), respondInvite, pendingInvites, collaborations, collabMessages, sendCollabMessage (broadcasts via socket), removeCollaborator; marketplace.verify/verification/verificationPublic added; publish runs verification after insert (insertId extracted)
- [x] Projetos.tsx rewritten: invites banner, collab panel (invite/search, collaborators list + remove, live chat with socket project:message, project:missionUpdate refetches projects)
- [x] Marketplace.tsx: verification badge on cards (preloads via verificationPublic cycling query, map by id) and detail dialog (checks list)
- [x] client/public/manifest.webmanifest + sw.js created (data:svg icons 192/512); index.html has manifest link + PWA meta tags
- [x] tsc passes (0 errors)

## REMAINING
- [x] SW registered in main.tsx
- [x] Socket client join in Projetos.tsx
- [x] 48/48 vitest tests passing
- [ ] Cleanup: remove server/seed-verifications.mjs after use (one-off script, must not ship)
- [ ] Run seed-verifications.mjs to create verification rows for 5 real plugins (via npx tsx)
- [ ] Screenshot: verify PROJETO card collab panel opens + marketplace verification badge shows on cards
- [ ] Checkpoint + sync README to GitHub (python sync script style: use gh contents API or /tmp/gh_sync.py; PAT fine-grained; skip .project-config.json)
- [ ] Final delivery message (features: collab rooms w/ validation, chat, invites, verification 5 checks, PWA manifest+sw+registration)

## Status so far
- [x] Schema: projectCollaborations, collaborationMessages, pluginVerifications added + migration 0011 applied to DB
- [x] db.ts: helpers added — getCollaborators, inviteCollaborator, respondToInvite, listPendingInvites,
  listProjectCollaborations, getCollaborationMessageHistory, addCollaborationMessage, removeCollaborator,
  verifyPluginSource (pure fn, 5 checks: source present, no hardcoded secrets ghp_/re_/sk-/AIza, balanced braces/parens,
  export signature, <200KB), createPluginVerification (deletes old rows first), getLatestPluginVerification
- [x] routers.ts: marketplace.publish now runs verification after addMarketplacePlugin; added marketplace.verify
  (mutation, protected), marketplace.verification (protected query), marketplace.verificationPublic (public query).
  addMarketplacePlugin returns MySQL insert result (array with insertId) — verify router's createPluginVerification
  uses input.pluginId directly, so publish needs the insertId extracted.

## Remaining backend
- [ ] Fix publish router: extract pluginId properly (result[0] insertId) and run verification
- [ ] Add collaboration router (projects router or new): invite, respond, pendingInvites, listCollaborators,
  projectMessages history/send, removeCollaborator — endpoints:
  projects.inviteCollaborator, projects.collaborations (list by projectId), projects.respondInvite,
  projects.pendingInvites, projects.collabMessages (query history), projects.sendCollabMessage (mutation),
  projects.removeCollaborator
- [ ] socket.ts: join room `project:<projectId>` for collaborators; on sendCollabMessage + mission status change,
  emit to room. Check socket.ts structure first (likely has rooms for users already).
- [ ] PWA static endpoints: serve /manifest.webmanifest + /sw.js from client/public (create both files)
  + icons in client/public (use generated small png icons; keep <100KB each, upload via manus-upload-file
  OR generate tiny base64 icons inline in manifest with "icons" as data: URLs)

## PWA files to create
- client/public/manifest.webmanifest (name NEXUS, theme #0a0f1a-ish cyberpunk, colors, data: icons)
- client/public/sw.js (cache-first for static assets + network-first for /api/trpc)
- client/src/lib/pwa.ts (register SW once, helper isPWA)
- Update client/index.html with <link rel="manifest"> and apple-touch-icon
- Add isPWA check + install hint banner component (optional)

## Frontend pages to edit
- client/src/pages/Projetos.tsx — add "Colaboração" tab: invite by user id/email (projects.findUsers exists),
  accept/reject invites (listPendingInvites), show collaborators + live chat box (sendCollabMessage) with
  socket event 'projectMessage'.
- client/src/pages/Marketplace.tsx (and MarketplaceDetails if exists) — add "Verificado" badge (ShieldCheck icon,
  emerald) using marketplace.verificationPublic query; tooltip listing checks.
- client/src/App.tsx — ensure route exists for /colaboracao? Or keep within Projetos. Keep simple: within Projetos.

## Socket.io notes (check server/socket.ts)
- Namespace/io setup; rooms per user likely `user:<userId>`. Add `project:<projectId>` room:
  on client connect if socket.handshake.query has projectIds -> join rooms. Or server emits
  to rooms and client joins on socket connection.
- New server emit events: 'projectMessage' (payload {projectId, userId, userName, content, createdAt}),
  'projectMissionUpdate' (payload {projectId, missionId, status, userId}).

## Tests (vitest): add to server/nexus.test.ts
- verifyPluginSource: valid plugin -> verified, secret -> failed, unbalanced -> failed, no export -> failed
- Collaboration: invite/accept round-trip, respondToInvite ownership, listPendingInvites, removeCollaborator
- marketplace.verification query

## File locations for quick re-read
- socket.ts: server/socket.ts (join/leave + broadcasts)
- collaboration UI: client/src/pages/Projetos.tsx (full rewrite done)
- verification UI: client/src/pages/Marketplace.tsx (~line 60-122 queries; badges line ~340 and ~410)
- PWA: client/public/manifest.webmanifest, client/public/sw.js, client/index.html (done), register in main.tsx (pending)

## Notes on existing patterns
- tRPC routers in server/routers.ts (monolithic, ~1000 lines)
- Protected via protectedProcedure; adminProcedure exists
- getDb exported from server/db.ts
- socket server at server/socket.ts, client connects in pages using io(url)
- DB: MySQL, drizzle, schema drizzle/schema.ts
- 41 tests passing previously (server/auth.logout.test.ts + server/nexus.test.ts)
- GitHub repo: Robsonvilares33/nexus-infraestrutura-cognitiva-universal (PAT lacks git-push; use python /tmp/gh_sync.py style API sync, skip .project-config.json and .session-notes.md)
