# Fase 22 — Notas de progresso (persistir até checkpoint)

## Status atual (16/08/2026 ~21:25 BRT)
- Backend Phase 22 COMPLETO:
  - `server/nexus-loterias.ts`: fetchDrawFromCaixa (retry 3x backoff), findLatestDrawNumber (busca binária — endpoint `/0` instável retorna 500), collectAndPersist, computeStats, generateStatisticalBet, mulberry32.
  - `server/routers/loterias.ts`: router `loterias` com list, counts, collectStatus, collect (mutation), draws, stats, generateBets. Registrado em `server/routers.ts` linha ~1415.
  - `server/db.ts`: insertLotteryDraw, listLotteryDraws, countLotteryDraws.
  - `server/_core/index.ts` linhas 234-273: callback heartbeat `/api/scheduled/loterias-collect` (14:05 UTC, coleta 5 últimos de cada uma das 5 loterias).
  - Migration 0019 (tabela lotteryDraws) APLICADA no banco (erro anterior: sql foi gerado em drizzle/0019_wooden_blue_marvel.sql, não em migrations/ — mas o SQL foi aplicado via webdev_execute_sql na segunda tentativa? NÃO — o segundo apply falhou com "already exists"; tabela JÁ EXISTIA no banco desde início).
  - Backfill: 30 linhas (6 sorteios mais recentes de cada 5 loterias). megasena até 3045, quina 7093, lotofacil 3763, lotomania 2963, timemania 2424.
  - Vitest: `server/nexus-loterias.test.ts` — 18/18 passando.
- Frontend Phase 22 COMPLETO:
  - `client/src/pages/Loterias.tsx` criado (KPIs, últimos sorteios com bolinhas, tabs frequência/atraso/pares, minicards quentes/frias/atraso, gerador de apostas com diálogo).
  - Rota `/loterias` registrada em `App.tsx`; item de navegação "Loterias" (TrendingUp) adicionado ao NAV_ITEMS de NexusLayout.
  - TypeScript limpo; screenshot verificado — página renderiza com dados reais.
- TODO.md Fase 22: itens backend + vitest marcados [x]; pendentes: [ ] Frontend página Loterias (agora pronto), [ ] README seção Fase 22, [ ] Sync GitHub (branch sync-fase22b + PR #11).

## Dados CAIXA (verificados)
- API pública: https://servicebus2.caixa.gov.br/portaldeloterias/api/{loteria}/{concurso}
- Endpoint `/0` retorna 500 (instável) — usar findLatestDrawNumber com busca binária.
- Rate: ~1s/req. Headers: User-Agent Mozilla, Referer loterias.caixa.gov.br.
- Últimos números: megasena 3045 (16/08/2026), quina 7093, lotofacil 3763, lotomania 2963, timemania 2424.

## Próximos passos
1. Marcar frontend [x] no todo.md.
2. Rodar `pnpm test` completo (todos os arquivos) antes do checkpoint.
3. Adicionar seção Fase 22 no README.md.
4. webdev_save_checkpoint.
5. GitHub: criar branch sync-fase22b, commit, push, PR #11 em https://github.com/Robsonvilares33/nexus-infraestrutura-cognitiva-universal
6. Deploy automático está ATIVO (checkpoint = publish).
7. Heartbeat diário: após publicar, criar cron project-level:
   `manus-heartbeat create --name loterias-daily-collect --cron "0 5 14 * * *" --path /api/scheduled/loterias-collect --description "Coleta diária 14:05 UTC (11h BRT) dos resultados da Caixa"`
