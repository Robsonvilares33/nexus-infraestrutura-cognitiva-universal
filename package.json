{
  "name": "nexus-infraestrutura-cognitiva-universal",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "api": "node --no-warnings server/index.js",
    "dev:full": "concurrently -n API,WEB -c cyan,magenta \"npm run api\" \"npm run dev\"",
    "local:doctor": "node --no-warnings server/doctor.js",
    "start": "node --no-warnings server/index.js",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "dependencies": {
    "concurrently": "^10.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "vite": "^7.0.2"
  }
}
