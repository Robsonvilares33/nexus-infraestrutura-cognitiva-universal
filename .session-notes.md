# Session Notes — Phase 5 (Feedback & Administration)

## Goal
Implement suggested next steps: marketplace reviews, admin panel, Analytics PDF export.

## Done so far (Phase 5)
1. Schema: `marketplaceReviews` + `marketplaceInstalls` tables added to drizzle/schema.ts; migration 0006 generated and APPLIED via webdev_execute_sql.
2. db.ts helpers added at end: addMarketplaceReview, getMarketplaceReviews, ensureMarketplaceInstall, listAllUsers, updateUserRole, setMarketplacePluginApproved, listAllMarketplacePlugins, deleteAnyMarketplacePlugin, getPlatformStats.
3. routers.ts: imported adminProcedure + new helpers; added marketplace.addReview, marketplace.reviews; added full admin router (stats, listUsers, setRole, listPlugins, approvePlugin, deletePlugin).
4. Owner user (id=1) promoted to admin via SQL.
5. Marketplace.tsx: reviews UI in detail dialog — star selector, average rating display, review list, addReviewMutation.
6. Admin.tsx page created (stats cards, plugin moderation, user management).

## Remaining
- App.tsx route /admin + NexusLayout nav item (admin-only visible) — need to import ShieldCheck; nav item can always show but page checks role (page itself gates).
- Analytics PDF export: server-side. Consider using html2canvas/jsPDF client-side instead (simpler, no extra runtime). Prefer client-side: build printable div + window.print styled, or jsPDF + html2canvas via manus-upload? Client-side window.print with @media print CSS is simplest and deployment-safe.
- Vitest for reviews + admin (test user role is "user" in tests → admin endpoints will throw FORBIDDEN; either create admin context helper or assert FORBIDDEN).
- TypeScript + pnpm test + checkpoint + report.

## Key facts
- adminProcedure exists in server/_core/trpc.ts (lines 28-30 area), requires ctx.user.role === 'admin'.
- Test ctx user role = "user" by default.
- Category colors: cyan #7cf3ff, purple #c9b8ff, gold #ffd479, green #3fe7b0, red #ff6b6b/#ff7a8c.
- Font-mono small text pattern: text-[10px] font-mono; labels text-[9px] text-[#7684a0] tracking-wider uppercase.
- nexus-card class for cards.
- Marketplace detailId state + reviews query already wired in Marketplace.tsx.
- TSC passes currently. Prior checkpoint: 3296c0c2 (22 tests).

## Phase 6 Progress (Semantic Search, Profile, Webhooks)

DONE:
- schema: userProfiles + missionWebhooks tables; migration 0007 applied.
- db.ts: getUserProfile, upsertUserProfile, getUserMissionsHistory, getUserInstalledPlugins, getUserMarketplaceInstalls, getUserReviews, getUserSharedProjects (uses projectShares.sharedByUserId), add/list/removeMissionWebhook, fireMissionWebhooks (POST JSON, 10s timeout, updates lastStatus).
- routers.ts: profile (get/update/history), webhooks (add/list/remove); missions.execute fires fireMissionWebhooks on completion (fire-and-forget). All imported in db.ts imports.
- Profile.tsx page created: bio, avatar URL, accent color picker (5 colors), history cards (missões, plugins instalados + marketplace installs, avaliações, projetos compartilhados). Route /profile added to App.tsx; nav item "Perfil" (UserCircle2 icon) added to NexusLayout NAV_ITEMS.
- MinhaIA.tsx: Webhook button on completed mission cards, dialog with add/remove + HTTP lastStatus chips. Hooks ordering preserved (all useQuery/useMutation after scheduledMissions query, useState before/after grouped correctly). TSC OK.

TODO next:
- Semantic search in Marketplace: no built-in embedding API found (llm.ts has no embedding; dataApi.ts no embedding). Plan: server-side hybrid — Marketplace semantic search via LLM-generated keywords fallback: use invokeLLM with JSON output to extract relevant keywords from query, then do SQL LIKE matching on name/description weighted. OR simple TF approach. Implementation: marketplace.search semantic endpoint that calls invokeLLM to get terms array, then query with OR LIKE. Add marketplace.search route. UI: search box sends to semantic when "busca semântica" active; simplest: always route query through backend list with query (keep LIKE) and add a new "semantic" list option.
- Vitest: profile.get/update, webhooks.add/list/remove (need mission id from test user).
- Screenshots: /profile, /marketplace, /minhaia; then checkpoint + report.
- Prior checkpoint: 57ab1f57. Tests were 26 passing.
- Category colors: cyan #7cf3ff, purple #c9b8ff, gold #ffd479, green #3fe7b0, red #ff6b6b/#ff7a8c. Card: nexus-card. Labels: text-[9px] font-mono text-[#7684a0] tracking-wider uppercase.

## Status after Phase 6 screenshots (31 tests passing)
/profile renders well (Perfil, history cards). /marketplace renders with SEMÂNTICA button. /minhaia returned 404 — the actual route path in App.tsx is different (check: probably /minha-ia or /missions). Verify exact path and re-screenshot the correct one for webhook UI.
Semantic search implemented server-side: marketplace.semanticSearch (LLM gpt-5-mini term expansion + SQL OR LIKE on name/description, approved only). UI toggle "SEMÂNTICA" in Marketplace.
Remaining: re-screenshot correta rota MinhaIA, mark todos, checkpoint.

## Phase 7 Progress (current)
Done: suggestedCategories table created (direct SQL + drizzle schema + migration 0008 generated). db.ts helpers added at end: addSuggestedCategory (dedupe), voteSuggestedCategory, listSuggestedCategories(approvedOnly), approveSuggestedCategory, deleteSuggestedCategory, getWeeklyGrowthStats(weeks), getUserById, BASE_CATEGORIES constant ["model","infra","device","utility"].
Email notification approach: notifyOwner() only notifies PROJECT OWNER via Manus platform (not per-user email). So implement: (1) in scheduled mission handler (server/_core/index.ts) + missions.execute webhook fire — call notifyOwner which delivers to the user as owner of the app (only viable channel). For plugin review notifications, same: notifyOwner with plugin+reviewer info. No per-user email API available (Gmail MCP requires interactive UI confirmation — not suitable server-side).
Remaining Phase 7: routers for categories (suggest/list approved for all; listAll+approve/delete for adminProcedure; vote), categories UI in Marketplace publish dialog + separate "Categorias da comunidade" section, admin growth charts (getWeeklyGrowthStats wired to admin.growth), notifyOwner calls in mission completion (already exists for scheduled; add for regular mission completion in missions.execute) and addReview, vitest, screenshots, checkpoint.
Admin router is in routers.ts under `admin:` key; marketplace under `marketplace:`.
Category filter Select in Marketplace uses hardcoded values — need to merge dynamic approved categories into publish dialog Select.

## Phase 7 State (before compaction)
DONE so far:
- suggestedCategories table (SQL + schema + migration 0008). db.ts helpers appended: addSuggestedCategory(dedupe), voteSuggestedCategory, listSuggestedCategories(approvedOnly), approveSuggestedCategory, deleteSuggestedCategory, getWeeklyGrowthStats(weeks), getUserById; BASE_CATEGORIES export.
- routers.ts: categories router added (listApproved, suggest, vote, listPending-admin gated); admin.growth (getWeeklyGrowthStats), admin.listCategories/approveCategory/deleteCategory added. Imports updated.
- missions.execute already calls notifyOwner on completion (line ~338-346, title "[NEXUS] Missão Concluída"). Scheduled handler in server/_core/index.ts also calls notifyOwner.
Email channel note: only notifyOwner (Manus platform push to project owner) is available; no per-user email API. Gmail MCP requires interactive UI confirmation so not usable from backend.

REMAINING:
1. Notify plugin author (notifyOwner) when a review is added — in marketplace.addReview (line ~703).
2. Marketplace.tsx: publish dialog category Select should merge approved community categories (trpc.categories.listApproved) + BASE ones; add "Categorias da comunidade" section with suggest+vote UI.
3. Admin.tsx: add growth charts tab/section using admin.growth (weeks: {week,newUsers,newMissions,newPlugins}) with Recharts LineChart.
4. Vitest: categories + admin.growth tests.
5. tsc + pnpm test + screenshots + checkpoint + report.
