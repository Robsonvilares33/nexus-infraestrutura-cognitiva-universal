# Phase 10 Progress Notes

## Done
- [x] Schema: `pluginThreads` (id, pluginId, authorId, parentId, content, createdAt) + `xp_events` (id, userId, source enum[plugin_publish=50, review=10, mission_complete=20, collab_accept=5], xp, createdAt) added to drizzle/schema.ts
- [x] Migration 0012_crazy_leopardon.sql generated + APPLIED to DB (CREATE TABLE both)
- [x] db.ts: createPluginThread, listPluginThreads (with authorNames), deletePluginThread (cascade replies, author-only), awardXp, XP_VALUES, getXpLeaderboard (SUM/COUNT groupBy limit 20, names), getMyXp. Import added `pluginThreads, xpEvents` + `or` from drizzle-orm. Array.from(new Set(...)) for downlevelIteration compat.
- [x] routers.ts: threads router (list public, create, remove) + leaderboard router (list public, my protected). XP awarded in marketplace.publish (plugin_publish) and marketplace.addReview (review). addFeedEvent accepts missionId already. TSC clean.
- [x] Marketplace.tsx: threads query + createThreadMutation/removeThreadMutation + threaded UI in detail dialog (roots + indented replies, responder toggle, remover only own). Textarea already imported.
- [x] Leaderboard.tsx page created (XP cards for me, top-20 rows w/ rank medals, XP rules legend) with import fix @/_core/hooks/useAuth
- [x] App.tsx: route /leaderboard -> Leaderboard; NexusLayout.tsx: nav item Leaderboard (Zap icon added to lucide import)

## Remaining
- [ ] SW offline cache for recent missions: add `caches.open("nexus-missions-v1")` cache on successful GET missions responses; also handle offline indicator — simplest: in sw.js add mission endpoint special handling (network-first with cache fallback + cache successful POST? No — only cache GET list). Also expose online/offline via a small helper component? Keep minimal: SW change + add online/offline badge in header optional.
- [ ] Award XP for mission_complete in mission execute flow (find where status=completed set, likely in routers.ts missions or db.ts updateMission) AND collab_accept in respondToInvite flow (routers collaboration.respond).
- [ ] Vitest coverage: threads (create/list/reply cascade/remove) + XP (leaderboard list, my xp, publish grants 50) — file server/nexus.test.ts, test user id 1, cleanup via removeMarketplacePlugin. Current count 48 tests passing.
- [ ] tsc + screenshots: /leaderboard, /marketplace (threads section). Cleanup DB if tests leak (vitest-% names) via webdev_execute_sql on pluginThreads/xp_events (they're test users id? tests create users via trpc caller—just delete xp_events where userId=1? NO — be careful: userId 1 is the real user; tests may use id 1. Better delete rows where source source values exist? Actually safe to not cleanup threads/xp test rows unless visible leak — leaderboard shows test data though; consider deleting after tests.)
- [ ] GitHub sync: python3 /tmp/gh_sync.py (syncs tree via Git Data API; skips .project-config.json, .session-notes.md; secret scanning rejects ghp_ tokens in any file — test file already sanitized with placeholder ***REMOVED***)
- [ ] README.md: add Phase 10 items to feature table (threads, leaderboard, offline missions)
- [ ] webdev_save_checkpoint at the end
- Repo: Robsonvilares33/nexus-infraestrutura-cognitiva-universal. Do NOT push real PAT (token). Sync script exists at /tmp/gh_sync.py.
- Last checkpoint: 85b00825
